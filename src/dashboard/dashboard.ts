// Dashboard entry point — Phase 1
// Implements: BroadcastChannel listener for metrics, fetch/invalidate triggers, DOM updates
// Full implementation in Plan 02 (mock server) + Plan 03 (LRU + metrics + dashboard)

export {}
